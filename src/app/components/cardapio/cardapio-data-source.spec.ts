import { CardapioDataSource } from './cardapio-data-source';

describe('CardapioDataSource', () => {
  it('should create an instance', () => {
    expect(new CardapioDataSource()).toBeTruthy();
  });
});
