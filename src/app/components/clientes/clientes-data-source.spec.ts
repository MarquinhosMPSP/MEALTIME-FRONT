import { ClientesDataSource } from './clientes-data-source';

describe('ClientesDataSource', () => {
  it('should create an instance', () => {
    expect(new ClientesDataSource()).toBeTruthy();
  });
});
