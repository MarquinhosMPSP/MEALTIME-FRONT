import { ReservasDataSource } from './reservas-data-source';

describe('ReservasDataSource', () => {
  it('should create an instance', () => {
    expect(new ReservasDataSource()).toBeTruthy();
  });
});
